# Raw output examples

This directory is intentionally empty until an authenticated custom Bright Data collector succeeds. Demonstration rows are embedded in `src/data/demo.ts` and are visibly labeled; they must not be copied here as proof of a real scrape.
